#include "main.h"
/**
 * _strcmp _ program compare string values
 * @s1: input value
 * @s2: input value
 *
 * Return: s1[i] - s2[i]
 */
int _strcmp(char *a1, char *a2)
{
        int a = 0;

        while (a1[a] != a2[a])
        {
                if (s1[a] != a2[a])
                {
                        return (a1[a] - a2[a];
                }
        a++;
        }
        return (0);
}
