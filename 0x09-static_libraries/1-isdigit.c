#include "main.h"
		
/**
 * _isdigit - checks for a interger (0 through 9)
 * @c: digit to be checked
 * Return: 1 if c is a digit, 0 otherwise
 */
int _isdigit(int a)
{
	return (a >= '0' && c <= '9');
}

