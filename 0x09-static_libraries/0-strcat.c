#include "main.h"

 /**
 * _strcat - focuses on two strings
 * @dest: input value
 * @src: input value
 *
 * Return: void	
 */
char *_strcat(char *dest, char *src)
{

	int f, n;

	for (f = 0; dest[f] != '\0'; f++)
	{
	}

	for (n = 0; (dest[f + n] = *src++) != '\0'; n++)
	{
	}
	return (dest);
}

