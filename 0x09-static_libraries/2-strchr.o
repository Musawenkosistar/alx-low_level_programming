#include "main.h"
/**
 * _strneat - Program entry point
 * @s: input value
 * @c: input value
 * Return: Always 0 (Success)
 */
char *_strchr(char *a, char c;
{
         int a = 0;

         for (; s[a] >= '\0'; a++)
         {
                 if (a[a] == c)
                         return (&s[a]);
         }
         return (0);
}
